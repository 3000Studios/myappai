{
"updatedFor": "myappai",
"slug": "react-vite-setup-2025",
"title": "How to Set Up a React + Vite Project in 2025 (The Right Way)",
"description": "A modern React + Vite setup guide covering project structure, ESLint, path aliases, environment variables, and deployment to Cloudflare Pages.",
"publishedAt": "2025-11-20T00:00:00.000Z",
"author": "MyAppAI Team",
"tags": ["React", "Vite", "tutorial", "frontend"],
"body": "Vite has become the standard build tool for React projects in 2025. It's faster than Create React App, more flexible than Next.js for pure SPAs, and pairs perfectly with Cloudflare Pages for deployment. Here's how to set up a production-ready React + Vite project from scratch.\n\n## 1. Create the Project\n\n```bash\nnpm create vite@latest my-app -- --template react\ncd my-app\nnpm install\n```\n\n## 2. Project Structure\n\nA clean structure for a medium-sized app:\n\n```\nmy-app/\n  src/\n    components/     # Reusable UI components\n    pages/          # Route-level page components\n    hooks/          # Custom React hooks\n    lib/            # Utilities and helpers\n    styles/         # Global CSS\n    App.jsx\n    main.jsx\n  public/\n  index.html\n  vite.config.js\n```\n\n## 3. Add React Router\n\n```bash\nnpm install react-router-dom\n```\n\nUpdate `src/main.jsx`:\n\n```jsx\nimport React from 'react'\nimport ReactDOM from 'react-dom/client'\nimport { BrowserRouter } from 'react-router-dom'\nimport App from './App.jsx'\nimport './styles/app.css'\n\nReactDOM.createRoot(document.getElementById('root')).render(\n  <React.StrictMode>\n    <BrowserRouter>\n      <App />\n    </BrowserRouter>\n  </React.StrictMode>\n)\n```\n\n## 4. Configure Path Aliases\n\nIn `vite.config.js`:\n\n```javascript\nimport { defineConfig } from 'vite'\nimport react from '@vitejs/plugin-react'\nimport path from 'path'\n\nexport default defineConfig({\n  plugins: [react()],\n  resolve: {\n    alias: {\n      '@': path.resolve(__dirname, './src'),\n      '@components': path.resolve(__dirname, './src/components'),\n      '@pages': path.resolve(__dirname, './src/pages'),\n    }\n  }\n})\n```\n\nNow you can import with `import Button from '@components/Button'` instead of relative paths.\n\n## 5. Environment Variables\n\nVite uses `.env` files. Variables must be prefixed with `VITE_` to be accessible in the browser:\n\n```bash\n# .env.local\nVITE_API_URL=https://api.example.com\nVITE_ENABLE_ANALYTICS=true\n```\n\nAccess them in code:\n\n```javascript\nconst apiUrl = import.meta.env.VITE_API_URL\n```\n\n## 6. ESLint Setup\n\n```bash\nnpm install -D eslint @eslint/js eslint-plugin-react eslint-plugin-react-hooks\n```\n\nCreate `eslint.config.js`:\n\n```javascript\nimport js from '@eslint/js'\nimport reactPlugin from 'eslint-plugin-react'\nimport reactHooks from 'eslint-plugin-react-hooks'\n\nexport default [\n  js.configs.recommended,\n  {\n    plugins: { react: reactPlugin, 'react-hooks': reactHooks },\n    rules: {\n      'react-hooks/rules-of-hooks': 'error',\n      'react-hooks/exhaustive-deps': 'warn',\n    }\n  }\n]\n```\n\n## 7. Deploy to Cloudflare Pages\n\nAdd `wrangler.toml`:\n\n```toml\nname = \"my-app\"\npages_build_output_dir = \"dist\"\n```\n\nDeploy:\n\n```bash\nnpm run build\nnpx wrangler pages deploy dist --project-name=my-app --branch=main\n```\n\n## 8. Handle Client-Side Routing\n\nFor SPAs with React Router, add a `public/_redirects` file so Cloudflare serves `index.html` for all routes:\n\n```\n/* /index.html 200\n```\n\n## Common Mistakes to Avoid\n\n- **Don't use CRA in 2025** — it's unmaintained and slow\n- **Don't commit `.env.local`** — add it to `.gitignore`\n- **Don't skip the `_redirects` file** — your routes will 404 on refresh\n- **Don't use default exports everywhere** — named exports are easier to refactor\n\nThis setup gives you a fast, modern React app that's ready to deploy in minutes."
}